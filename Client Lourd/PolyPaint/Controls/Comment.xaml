<UserControl x:Class="PolyPaint.Comment"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PolyPaint"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="150"
             MinHeight="100" MinWidth="150" >
    <Grid  x:Name="myGrid">
        <TextBox x:Name="myTextBox" Height="{Binding Path=Height, ElementName=myGrid}"
                    Width="{Binding Path=Width, ElementName=myGrid}"
                 Background="Yellow" Margin="3"
                 MouseLeave="TextBox_MouseLeave" TextWrapping="WrapWithOverflow"
                 AcceptsReturn="True"
                 HorizontalScrollBarVisibility="Auto"/>
    </Grid>
</UserControl>
